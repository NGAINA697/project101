#include <stdio.h>

int main() {
    int age
    printf ("Enter age here: ");
    scanf ("%d",age);
    if(age >=18){
        printf ("Allow");
    }
    else{
        printf ("Deny")
    }
}